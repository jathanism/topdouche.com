$(function() {
    // Make tables that are sortable; sortable.
    $("table.sortable").tablesorter(); 

    // Fade in form input on page load because it looks cool.
    $('form').hide().fadeIn();

});
